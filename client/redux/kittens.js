
// ACTION TYPES


// ACTION CREATORS


// INITIAL STATE
const initialState = {
  kittens: [],
  kitten: {}
}

// REDUCER
const kittensReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

// THUNKS

export default kittensReducer;
