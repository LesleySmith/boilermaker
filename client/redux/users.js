
// ACTION TYPES


// ACTION CREATORS


// INITIAL STATE
const initialState = {
  users: [],
  user: {}
}

// REDUCER
const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

// THUNKS

export default userReducer;
